version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "2000:2000"
    environment:
      - PORT=2000

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    environment:
      - REACT_APP_API_URL=http://backend:2000
